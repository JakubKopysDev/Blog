<%= form_for post do |f| %>
    <div class="field clearfix">
      <%= f.label :title %>
      <%= f.text_field :title %>
    </div>

    <div class="field clearfix">
      <%= f.label :content %>
      <%= f.text_area :content %>
    </div>

    <%= f.submit %>
<% end %>